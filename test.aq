
type MyObject {
    let num: int;
}

impl MyObject: Freeable {
    def free() {
        
    }
}

type MyOtherObject {
    let vector: Vec[int];
}

let box = Box[int].new(MyObject(5));

print(*box.as_ptr());

let obj = MyOtherObject(Vec[int].new());

